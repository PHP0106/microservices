spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/inventory-service
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none

  application:
    name: inventory-service

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@localhost:8761/eureka


